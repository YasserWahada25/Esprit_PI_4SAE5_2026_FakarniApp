apiVersion: apps/v1
kind: Deployment
metadata:
  name: activite-educative-service
  namespace: fakarni
spec:
  replicas: 1
  selector:
    matchLabels:
      app: activite-educative-service
  template:
    metadata:
      labels:
        app: activite-educative-service
    spec:
      containers:
        - name: activite-educative-service
          image: didou2505/fakarni-activite-educative-service:latest
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "docker"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: fakarni-config
                  key: EUREKA_URL
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://db-activite-educative:3306/activite_educative_db?createDatabaseIfNotExist=true&serverTimezone=UTC"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fakarni-secrets
                  key: MYSQL_ROOT_PASSWORD
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: activite-educative-service
  namespace: fakarni
spec:
  selector:
    app: activite-educative-service
  ports:
    - port: 8084
      targetPort: 8084
  type: ClusterIP
