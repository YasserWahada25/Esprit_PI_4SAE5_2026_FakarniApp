apiVersion: apps/v1
kind: Deployment
metadata:
  name: suivi-engagement-service
  namespace: fakarni
spec:
  replicas: 1
  selector:
    matchLabels:
      app: suivi-engagement-service
  template:
    metadata:
      labels:
        app: suivi-engagement-service
    spec:
      containers:
        - name: suivi-engagement-service
          image: didou2505/fakarni-suivi-engagement-service:latest
          ports:
            - containerPort: 8088
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "docker"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: fakarni-config
                  key: EUREKA_URL
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://db-suivi-engagement:3306/suivi_engagement_db?createDatabaseIfNotExist=true&serverTimezone=UTC"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fakarni-secrets
                  key: MYSQL_ROOT_PASSWORD
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: suivi-engagement-service
  namespace: fakarni
spec:
  selector:
    app: suivi-engagement-service
  ports:
    - port: 8088
      targetPort: 8088
  type: ClusterIP
