# Copier en .env à la racine FakarniApp :  copy env.docker.example .env
MYSQL_ROOT_PASSWORD=root
MYSQL_USER=root
# User-Service (Mongo) — hôte = nom du service compose « mongo »
USER_MONGODB_URI=mongodb://mongo:27017/rayen
