# Profil local : sans Eureka (ex. mvn spring-boot:run -Dspring-boot.run.profiles=local)
# Sans MySQL : ajouter aussi le profil « h2 » → -Dspring-boot.run.profiles=local,h2
spring.cloud.discovery.enabled=false
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
